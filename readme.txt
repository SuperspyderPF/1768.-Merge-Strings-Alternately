The problem requires merging two strings by adding letters alternately. 
We can achieve this by iterating over the strings and appending letters alternatively until one of the strings ends, 
and then appending the remaining letters of the other string to the merged string.